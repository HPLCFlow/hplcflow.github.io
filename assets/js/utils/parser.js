class ProtocolParser{constructor(protocolDefinition){this.protocol=protocolDefinition}parse(buffer){let result={};let offset=0;let bufferView=new DataView(buffer);for(let field of this.protocol.fields){try{let fieldResult=this.parseField(field,bufferView,offset,result);result[field.name]=fieldResult.value;offset=fieldResult.newOffset}catch(e){console.error(`Error parsing field ${field.name}: ${e.message}`);throw e}}return result}parseField(field,bufferView,offset,currentResult){switch(field.type){case"fixed":return this.parseFixedField(field,bufferView,offset);case"bytes":return this.parseBytesField(field,bufferView,offset,currentResult);case"bits":return this.parseBitsField(field,bufferView,offset);case"dynamic":return this.parseDynamicField(field,bufferView,offset,currentResult);case"checksum":return this.parseChecksumField(field,bufferView,offset,currentResult);default:throw new Error(`Unknown field type: ${field.type}`)}}parseFixedField(field,bufferView,offset){const expectedValue=parseInt(field.value,16);const actualValue=bufferView.getUint8(offset);if(actualValue!==expectedValue){throw new Error(`Fixed field ${field.name} expected ${field.value} but got ${actualValue.toString(16)}`)}return{value:field.value,newOffset:offset+field.size}}parseBytesField(field,bufferView,offset,currentResult){let size=field.size;if(size==="dynamic"&&field.determinedBy){size=currentResult[field.determinedBy]}let bytes=[];for(let i=0;i<size;i++){bytes.push(bufferView.getUint8(offset+i).toString(16).padStart(2,"0"))}let value;switch(field.format){case"bcd":value=this.parseBcd(bytes);break;case"integer":value=this.parseInteger(bytes,field.endianness);break;case"float":value=this.parseFloat(bytes,field.endianness);break;default:value=bytes.map(b=>b.toString(16).padStart(2,"0")).join(" ")}return{value:value,newOffset:offset+size}}parseBitsField(field,bufferView,offset){const byte=bufferView.getUint8(offset);let result={};for(let bitField of field.bitFields){const mask=(1<<bitField.bits)-1;const value=byte>>bitField.position&mask;result[bitField.name]=value}return{value:result,newOffset:offset+field.size}}parseDynamicField(field,bufferView,offset,currentResult){if(field.reference&&field.operation==="length"){const refValue=currentResult[field.reference];const length=refValue.length;return{value:length,newOffset:offset+field.size}}throw new Error(`Unsupported dynamic field configuration`)}parseChecksumField(field,bufferView,offset,currentResult){let sum=0;const startIndex=this.getFieldOffset(field.range[0]);const endIndex=this.getFieldOffset(field.range[1])+this.getFieldSize(field.range[1]);for(let i=startIndex;i<endIndex;i++){sum+=bufferView.getUint8(i)}const checksum=bufferView.getUint8(offset);const isValid=(sum&255)===checksum;return{value:{calculated:sum&255,actual:checksum,valid:isValid},newOffset:offset+field.size}}parseBcd(bytes){let result="";for(let byte of bytes){result+=(parseInt(byte,16)>>4).toString();result+=(parseInt(byte,16)&15).toString()}return result}parseInteger(bytes,endianness="big"){let result=0;if(endianness==="big"){for(let i=0;i<bytes.length;i++){result=result<<8|parseInt(bytes[i],16)}}else{for(let i=bytes.length-1;i>=0;i--){result=result<<8|parseInt(bytes[i],16)}}return result}getFieldOffset(fieldName){return 0}getFieldSize(fieldName){return 1}}async function loadAndParseProtocol(protocolFile,buffer){try{const response=await fetch(protocolFile);const protocolDef=await response.json();const parser=new ProtocolParser(protocolDef);const result=parser.parse(buffer);console.log("Parsed result:",result);return result}catch(error){console.error("Error loading or parsing protocol:",error);throw error}}document.addEventListener("DOMContentLoaded",async()=>{console.log("docs/tools/command-parser/script.js")});