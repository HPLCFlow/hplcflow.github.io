import{analyzeMachineLog}from"./machineLog.js";import{analyzeModuleLogCCO}from"./moduleLogCCO.js";import{analyzeModuleLogSTA}from"./moduleLogSTA.js";document.addEventListener("DOMContentLoaded",function(){document.getElementById("log-file").addEventListener("change",function(event){const file=event.target.files[0];if(file){const fileType=getFileType(file.name);const fileSize=getFileSize(file.size);document.getElementById("file-type").value=fileType;document.getElementById("file-size").value=fileSize}else{document.getElementById("file-type").value="";document.getElementById("file-size").value=""}});function getFileType(fileName){const extension=fileName.split(".").pop().toLowerCase();switch(extension){case"txt":return"文本文件";case"bin":return"二进制文件";default:return"未知文件类型"}}function getFileSize(size){if(size<1024){return size+" B"}else if(size<1024*1024){return(size/1024).toFixed(2)+" KB"}else if(size<1024*1024*1024){return(size/(1024*1024)).toFixed(2)+" MB"}else{return(size/(1024*1024*1024)).toFixed(2)+" GB"}}document.getElementById("analyze-log-btn").addEventListener("click",function(){const logFileInput=document.getElementById("log-file");const logTypeSelect=document.getElementById("log-type");const file=logFileInput.files[0];const logType=logTypeSelect.value;if(!file){updateStatus("请选择一个日志文件");return}updateStatus(`正在解析 ${logType} 日志...`);const reader=new FileReader;reader.readAsArrayBuffer(file);reader.onload=function(event){const fileContent=event.target.result;parseLogFile(fileContent,logType);updateStatus(`解析完成，结果显示在下方`)}});function updateStatus(message){document.getElementById("status-bar").textContent=message}function parseLogFile(fileContent,logType){let result="";const dataSection=document.getElementById("dataSection");if(logType==="machine-log"){result=analyzeMachineLog(fileContent)}else if(logType==="module-log-cco"){result=analyzeModuleLogCCO(fileContent)}else if(logType==="module-log-sta"){result=analyzeModuleLogSTA(fileContent)}dataSection.style.display="block";return result}});